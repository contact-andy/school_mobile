import React, { Component } from 'react';
import {
  StyleSheet,
  Text,
  View,
  TouchableOpacity,
  Image,
  Alert,
  ScrollView,
  FlatList,
} from 'react-native';

const SchoolInformation = () => {
  return ( 
    <View style={StyleSheet.container}>
      <Text>SchoolInformation</Text>
    </View>
   );
}
 
export default SchoolInformation;

const styles = StyleSheet.create({
  container:{
    flex:1,
    alignItems:'center',
  }
})